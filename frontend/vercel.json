{
    "version": 2,
    
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ]
  ,
    "builds": [
      { "src": "vite.config.js", "use": "@vercel/static-build" }
    ],
    "staticOutputDirectory": "dist"
  }
  